import EventList from "../eventList/EventList";

const Events = () => {
    return (
        <div>
            <h1> Events page</h1>
            <EventList/>
        </div>
    );
}

export default Events;